<p>注意：更新ボタンは押さないでください。<p>

<div class="total">
  不一致の件数は<a class='count'><%= @count %></a>件です。
</div>

<% @subject_numbers.each do |num| %>
  <p><%= num %></p>
<% end %>

<% @excel_data.each do |subject| %>
  <br>
  <table border="1" style="border-collapse: collapse">
    <thead>
      <tr>
        <th>対象</th>
        <th>PDF</th>
        <th>Excel</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><%= subject %></td>
        <td><%= subject %></td>
        <td><%= subject %></td>
      </tr>
    </tbody>
  </table>
  <br>
<% end %>

<div class="back-btn">
<%= link_to moca_data_path, class: 'btn btn_back' do %>
  <i class="fa-solid fa-arrow-left"></i>ファイル選択画面に戻る
<% end %>
</div>

<script type="module">
  import "result";
</script>