<p>PDFファイルとExcelファイルを1つずつ選択してください。</p>

<div>
  <%= form_with url: "/subject_data", method: :post, enctype: "multipart/form-data", data: { turbo: false } do |form| %>
    <p>
      <label for="uploads">ファイルを選択してください：</label>
      <%= form.file_field :uploads, name: "uploads[]", multiple: true, accept: ".xlsx, .pdf" %>
    </p>
    <p>
      <%= form.submit "送信"%>
    </p>
  <% end %>
</div>